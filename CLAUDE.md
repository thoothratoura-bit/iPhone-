# CLAUDE.md

<!--
運用メモ（この コメントはClaudeのコンテキストには読み込まれない）:
- このファイルは常に200行以下・できれば80行以下を維持する
- 追記の判定基準: 「この行を削除したらClaudeはミスするか？」→ No なら書かない
- コードを読めば分かること・言語標準の慣習・自明な良習慣は書かない
-->

## プロジェクト概要

このリポジトリは初期構築中で、まだコードがない。目的・技術スタックが決まったら、
**他のどの作業よりも先に**このセクションを実態に合わせて更新すること。

- 目的: 未定（決定次第記載）
- 技術スタック: 未定（決定次第記載）

## 作業原則

- 推測で前提を補わない。不明点は既存コード・git履歴・公式ドキュメントを先に確認し、
  それでも不明なら「不明」と明示する
- 変更後は必ず検証する。テスト・ビルド・lint のいずれかを実行し、
  実行したコマンドと結果（成功/失敗の出力）を報告する。
  検証手段がない場合は「未検証」と明示し、成功したと主張しない
- 複数ファイルにまたがる変更・設計判断を伴う変更は、実装前に計画を提示する。
  1文で差分を説明できる小さな修正は直接実装してよい
- 症状ではなく根本原因を修正する。エラーの握りつぶし・テストのスキップ・
  lint ルールの無効化で「解決」しない
- スコープ外の変更（頼まれていないリファクタ・整形・依存更新）を混ぜない

## コーディング規約

（スタック決定後、**デフォルトや標準と異なる規則のみ**をここに記載する）

## 開発コマンド

（プロジェクト初期化後に記載する。記載するのは Claude が推測できないコマンドのみ）

```
# 例（初期化後に実際のコマンドへ置き換える）:
# ビルド:  <未定>
# テスト:  <未定>
# lint:    <未定>
```

## Git 運用

- main に直接コミットしない。作業ブランチで開発する
- 1コミット = 1つの論理的変更。メッセージは変更の「なぜ」が分かるように書く
- コミット前に `git diff --staged` で意図しない変更が混ざっていないか確認する

## コンテキスト運用

- compact（会話圧縮）時は、変更したファイルの一覧・実行すべきテストコマンド・
  未完了のタスクを必ず保持する

## このファイル自体のメンテナンス

- 同じ訂正・指摘が2回発生したら、その内容を該当セクションに1行で追記する
- 「事実」ではなく「複数ステップの手順書」に育った項目は
  `.claude/skills/<name>/SKILL.md` へ移動する
- 特定のファイル種別・ディレクトリのみに適用される規則は
  `.claude/rules/`（`paths:` frontmatter 付き）へ移動する
- 古くなった規則・矛盾する規則は放置せず削除する。矛盾があると規則が恣意的に
  無視される
